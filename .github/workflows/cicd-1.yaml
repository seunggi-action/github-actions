name: cicd-1
on: 
  pull_request:
    types: [opened, synchronize, closded]
    branches: [dev]
    path: 
    - 'my-app/**'

  
jobs:
  test:
    if: github.event.action == 'opened' || github.event.actions == 'synchronize
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
  
  image-build:
    if: github.event.pull_request.merged == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
    
  deploy:
    runs-on: ubuntu-latest
    needs: image-build
    steps:
      - name: checkout
        uses: actions/checkout@v4